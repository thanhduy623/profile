<template>
    <div class="relative flex items-center my-10">
        <!-- Đường kẻ trái -->
        <div
            class="flex-1 h-[3px] bg-gradient-to-r from-transparent via-primary to-primary rounded-full shadow-[0_0_12px_rgba(59,130,246,0.6)]">
        </div>

        <!-- Biểu tượng giữa -->
        <div
            class="mx-6 flex items-center justify-center w-10 h-10 rounded-full bg-primary text-white shadow-[0_0_20px_rgba(59,130,246,0.8)] ring-4 ring-primary/20">
            ✦
        </div>

        <!-- Đường kẻ phải -->
        <div
            class="flex-1 h-[3px] bg-gradient-to-l from-transparent via-primary to-primary rounded-full shadow-[0_0_12px_rgba(59,130,246,0.6)]">
        </div>
    </div>
</template>